info:
  name: search
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/people/search?q=beethov
  params:
    - name: q
      value: beethov
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
